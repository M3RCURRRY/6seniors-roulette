import { createTheme } from "@mui/system";

const theme = createTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#000000',
    },
    secondary: {
      main: '#99e410',
    },
  },
})

export default theme;